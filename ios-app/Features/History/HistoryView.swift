import SwiftUI

/// Verlauf/Fahrtenliste (App Bible 6.5). Gesamtübersicht + chronologische Liste,
/// Wisch-Löschen; Tap -> RideDetailView.
struct HistoryView: View {
    var body: some View {
        NavigationStack { Text("Verlauf — TODO").navigationTitle("Verlauf") }
    }
}
