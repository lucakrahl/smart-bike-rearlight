import SwiftUI

/// Einstellungen (App Bible Kap. 7): Gerät · Cockpit · Anzeige · Info (Datenschutz lokal-only).
struct SettingsView: View {
    var body: some View {
        NavigationStack { Text("Einstellungen — TODO").navigationTitle("Einstellungen") }
    }
}
