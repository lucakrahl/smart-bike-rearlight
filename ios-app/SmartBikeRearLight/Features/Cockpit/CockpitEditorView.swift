import SwiftUI
import SmartBikeCore

/// Cockpit-Editor als Sheet (App Bible 6.4, UX-D). 3-Spalten-Raster, Größen-Chips,
/// Metrik-Wähler, „+ Kachel" / „Auf Standard zurücksetzen" / „Fertig".
struct CockpitEditorView: View {
    var body: some View { Text("Cockpit-Editor — TODO") }
}
