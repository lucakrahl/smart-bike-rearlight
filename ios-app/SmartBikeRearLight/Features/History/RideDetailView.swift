import SwiftUI

/// Fahrt-Detail (App Bible 6.5, AR-STAT-02/AR-VIS-*). Kennzahlen + Höhenprofil +
/// Geschwindigkeit (Swift Charts, über Distanz) + Routenkarte (MapKit, Should).
struct RideDetailView: View {
    var body: some View { Text("Fahrt-Detail — TODO") }
}
