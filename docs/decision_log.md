# Decision Log

> **Freigabepflichtig** (siehe `CLAUDE.md`): Neue Entscheidungen hier nur nach
> ausdrücklicher Freigabe des Nutzers ergänzen. Format je Eintrag: Entscheidung
> · Begründung · verworfene Alternative(n). Kanonische Fassung: Project Bible Kap. 10.

Auszug der zentralen Architekturentscheidungen (Vollständige Liste: Bible Kap. 10):

| Entscheidung | Begründung | Verworfene Alternative |
|---|---|---|
| Rechenlast in die Web-App (Variante 2) | ESP32 deterministisch, geringer RAM/CPU | Firmware rechnet alle Kennzahlen |
| Zustandsmodell als 4 parallele Regionen | additive Zustandsanzahl, testbar | flache kombinierte FSM |
| Kooperativer `millis()`-Scheduler | deterministisch, testbar, dokumentierbar | eigene FreeRTOS-App-Tasks im MVP |
| Rote LED = Schluss- + Bremslicht | § 67-konform + Bremslicht-Mehrwert | binäres Bremslicht |
| Notbrems-Blinken (ESS) experimentell/deaktiviert | Sicherheit vs. § 67 Abs. 4 | aktiv ausliefern (unzulässig) |
| Warnblinker per Langdruck (≥ 5 s) | ASK-Fernbedienung ohne Kombisignal | gleichzeitiges Tastendrücken |
| Trennung Logik ↔ Hardware | Host-Unit-Tests möglich | Logik an Treiber gekoppelt |
| No-OTA-Partition + NVS-Konfig | BLE-Firmware passt; NVS reicht | OTA-Schema / LittleFS |
| Build-Umgebung: PlatformIO mit pioarduino-Plattform (Arduino-ESP32-Core 3.3.x) | Core 3.x nötig für `ledcAttach` (CON-02); offizielle espressif32-Plattform liefert nur Core 2.0.17 | Arduino IDE / offizielle espressif32-Plattform (Core 2.x) |
| Zentrale I²C-Bus-Init (Anwendungsebene) | Modularität, Unabhängigkeit optionaler Sensoren, keine Reihenfolge-Abhängigkeit | `Wire.begin()` im Sensor-Treiber |
| BMP280 FORCED-Mode (Weather Monitoring ×1/×1/IIR aus) | geringes Rauschen @1 Hz, geringere Stromaufnahme, Bosch-Empfehlung | NORMAL-Dauerbetrieb ×16 |
| Keine feste Temperatur-Korrektur in der Firmware | Offset noch nicht abschließend charakterisiert, umgebungs-/lastabhängig; Rohdaten-Integrität; verfälscht sonst Druckkompensation | fester Offset im Code |
| Bremslicht nur bei Verzögerung in Fahrtrichtung (`MOTION_BRAKE_SIGN`, feldkalibriert) | Sprint/Beschleunigung darf kein Bremslicht auslösen; reale Einbaulage | \|a\| via `fabs()` (richtungsblind) |
| Getrennte IMU-Health-/Lifecycle-Init-Signale | `lifecycle_fsm.degraded` bleibt INIT-Ergebnis (FR-STA-06); Laufzeit-Sensorausfall gated nur R2 über ein eigenes Signal (`imu_health`) | `degraded` in `lifecycle_fsm` erweitern |
| WHO_AM_I-Liveness-Check statt `getEvent()`-Rückgabewert | Adafruit-Wrapper reicht I²C-Fehler nicht zuverlässig durch | `getEvent()`-Rückgabewert vertrauen |
| IMU-FSR ±16 g (statt Library-Default) | Fahrrad-Stöße bis ~20 g möglich, engeres FSR sättigt fälschlich | Default-Bereich (±8 g) belassen |
| Sprung-Plausibilität + Eskalations-Vertrauen (N konsekutive plausible Zyklen) | einzelnes Müll-aber-in-Range-Sample darf keine Bremseskalation auslösen | einzelnem plausiblen Sample sofort vertrauen |
| I²C-Bus-Recovery über rohe `gpio_*`-Calls statt `pinMode()`/`digitalWrite()` | PeriMan blockiert bei hängendem I²C-Bus selbst (verifiziert im Core-Quelltext) | Arduino-`pinMode()`/`digitalWrite()` |
| Board-Tausch auf Espressif ESP32-DevKitC-32E (WROOM-32E) | BLE-Brownout-Bootloop auf zehn systematischen Tests eingegrenzt; Root Cause = Regler des Altboards liefert die RF-Kalibrierungs-Transiente nicht; neues Board Referenz-Design, pin-kompatibel | Beim Altboard bleiben; WiFi statt BLE |
| Entkopplungskondensatoren (1000 µF an 3V3 und Vin) trotz Wirkungslosigkeit gegen den BLE-Brownout beibehalten | verbessern allgemeine Versorgungsstabilität/Transienten-Robustheit; weniger Rework-Risiko | Kondensatoren wieder entfernen |
| WiFi statt BLE verworfen | teilt denselben 2,4-GHz-Funk/dieselbe RF-Kalibrierung, mehr Strom → gleiches/stärkeres Brownout-Risiko; widerspricht NFR-PWR-01 | WiFi als Telemetrie-Transport |
| Bench-Validierung der Bremslicht-Logik über NFR-TST-02-Einspeisung (100-Hz-Serial-Log) | reproduzierbar, löst 300-ms-Halten und < 50-ms-Anstieg auf | physische Verzögerung / BLE-Telemetrie @ 10 Hz (zu grob) |
| **(06.08.2026)** Fusionsarchitektur **V-B**: IMU bleibt schneller Regelpfad, GNSS wird langsame Stützreferenz | GNSS erfüllt NFR-RT-01 (≤ 50 ms) physikalisch nicht (L86 max. 10 Hz; Gesamtlatenz 200–400 ms, 5–8× Vorgabe). Zusätzlich nicht integritätssicher: FIX_OK/HDOP ≤ 0,8 bei grob falscher Lösung. Die IMU liefert 100 Hz mit < 10 ms — sie war nie das Problem, nur ihre Signalaufbereitung | V-A (GNSS primär) — Latenz und Integritätsrisiko; V-C (IMU unverändert) — nachgewiesene Funktionsunfähigkeit |
| **(06.08.2026)** Normbetrags-Gate (drei Regime STATIC/DYNAMIC/SHOCK) statt Tiefpass oder Median | Nutzt den physikalischen Diskriminator ‖a‖ = g (Neigung) gegenüber ‖a‖ = √(g²+a²) (Bremsung) und ‖a‖ ≫ g (Stoß) statt einer Frequenztrennung, die es nicht gibt. Kostet keine Latenz, während ein 8-Hz-Tiefpass einen 25-ms-Stoß zu 71 % durchlässt. Etabliertes Verfahren (Madgwick 2010, Mahony et al. 2008) | Tiefpass auf a_y; Median-Filter; α anheben |
| **(06.08.2026)** GNSS-Stützung mit 5 Hz, L86 @ 115200 Bd | halbiert die Differenzierungslatenz gegenüber 1 Hz; 10 Hz bringt für τ_b ≈ 10 s keinen Nutzen, erhöht aber Strom und Parselast | 10 Hz; bei 1 Hz bleiben |
| **(06.08.2026)** GNSS-Stützung (Stufe 2) hinter Konfigurations-Flag, standardmäßig deaktiviert | Stufe 1 muss isoliert im Feld verifiziert werden — sonst wären bei erneutem Fehlbefund zwei Änderungen gleichzeitig wirksam | beide Stufen gleichzeitig aktivieren |
| **(06.08.2026)** Temperatur (`temperature_c`) bewusst nicht als Fahrtkennwert geführt | BMP280 sitzt im geschlossenen Gehäuse neben Leistungs-LED; misst Eigenerwärmung, nicht Umgebung. In der Firmware bleibt sie erhalten (Druckkompensation). Konsequenz: Spalte aus dem CSV-Export entfernen | Temperatur als Fahrtkennwert speichern und anzeigen (irreführend) |
| **(07.08.2026)** `MOTION_COMPL_TAU_S` 0,49 → 3,0 s | Der alte Wert stammte aus α = 0,98 @ 10 ms und war für dieses System nie begründet. Seit das explizite Anker-Fenster existiert, muss das laufende Filter die Startkonvergenz nicht mehr leisten; es führt nur noch Fahrbahnneigung nach (Sekunden bis Minuten). Sicherheitsprüfung: Ein Steigungssprung von 10 % = 5,7° erzeugt g·sin(5,7°) = 0,98 m/s², deutlich unter `BRAKE_ON_MS2` = 2,0. Senkt die Rampen-Absorption von 0,30 auf ≈ 0,06 m/s² | bei 0,49 s bleiben (Bremsungen knapp über der Schwelle werden nicht erkannt) |
| **(07.08.2026)** `MOTION_COMPL_TAU_SLOW_S` 30 → 90 s, mit Rückfallwert `_UNCAL_S` = 30 s | Erst durch die Bias-Kalibrierung zulässig: stationärer Fehler ε = b·τ = 0,005 °/s · 90 s = 0,45° = 0,077 m/s². Ohne Kalibrierung wären bei b = 0,5 °/s bis zu 45° möglich — daher der zwingende Rückfall, solange `bias_calibrated` = 0 | einheitlich 90 s (fahrlässig ohne Kalibrierung); bei 30 s bleiben (T11 unterschreitet die Ansprechschwelle) |
| **(07.08.2026)** SHOCK-Erkennung primär über die Änderungsrate (Jerk), nicht über den Betrag | Rückrechnung der ursprünglich betragsbasierten Schwellen ergab zwei Lücken: Bremsungen ≤ 2,64 m/s² blieben STATIC (genau der Bereich der verpassten Feldbremsungen), Bremsungen ≥ 7,44 m/s² wurden als SHOCK eingefroren. Über den Jerk trennen sich die Fälle um zwei Größenordnungen: Bremsung 0,08–0,34 m/s² je 10 ms gegenüber Schlagloch ≈ 20 | rein betragsbasierte Schwellen (0,35 / 2,5) |
| **(07.08.2026)** Verankerung nur aus `atan2f`, wenn das erste Sample im STATIC-Band liegt; sonst unverankert, Ausgang 0, Timeout 2 s → eben annehmen | Ein Filter ohne Historie kann bei Sample 1 „bremst bereits" nicht von „steht bereits schräg" unterscheiden. Ohne diese Regel setzt ein Kaltstart während einer Bremsung den Pitch auf den Bremswert und friert den Ausgang dauerhaft bei ≈ 0,3 m/s² ein | bedingungsloses `atan2f`-Seeding beim ersten Sample |
| **(07.08.2026)** Verankerung und Bias-Kalibrierung werden getrennt: Verankerung zusammenhängend (0,3 s, Toleranz drei Abtastungen), Bias kumulativ (200 STATIC-Abtastungen, ohne Zusammenhang) | Rücknahme der Vereinfachung „ein Zustand, zwei Verbraucher". Der Ruhetest zeigte 72 % STATIC-Anteil; eine zusammenhängende Sekunde ist damit praktisch unerreichbar (0,857¹⁰⁰ ≈ 2·10⁻⁷), `bias_calibrated` wurde nie 1. Physikalisch sind es zwei Anforderungen: Die Verankerung braucht eine kohärente Lagereferenz, die Bias-Mittelung nicht — einem Mittelwert ist Nachbarschaft gleichgültig. Genauigkeit: 30 Abtastungen → Ankerfehler ≈ 0,04° (0,007 m/s²); 200 Abtastungen → Restbias ≈ 0,0024 °/s (0,037 m/s² bei τ = 90 s) | gemeinsames Fenster beibehalten (funktioniert real nie) |
| **(07.08.2026)** Konvergenzkriterium für die Bias-Kalibrierung (Variante B2) verworfen | Es brächte zwei weitere geschätzte Parameter in ein System, das bereits drei ungemessene Schwellwerte trägt. Der gemessene Bias von −0,0014 °/s zeigt, dass Konvergenz praktisch kein Problem ist. Als Ausblick dokumentiert | B2 umsetzen |
| **(07.08.2026)** Telemetrie-Schema v3 überträgt Fensteraggregate statt Momentanwerte | Regime-Entscheidung und Jerk-Kriterium leben auf 100 Hz, das Frame läuft mit 10 Hz. Momentanwerte würden um Faktor zehn unterabtasten — gerade die Stöße, um deren Erkennung es geht, verschwänden. Aggregate (min/max/Zähler über 100 ms) erhalten die 100-Hz-Information ohne höhere Datenrate | Momentanwerte im Frame; Frame-Rate anheben (Bandbreite, Strom) |
| **(07.08.2026)** Ungültige GNSS-Referenz wird als `0.0f` plus separates Gültigkeitsflag übertragen, nicht als NaN | App-Decoder und CSV-Export müssten sonst einen NaN-Sonderfall über Swift, SwiftData und die deutsche Zahlformatierung durchreichen. Die Gültigkeit trägt ausschließlich `gnss_accel_valid` | NaN als Ungültigkeitsmarke |
| **(07.08.2026)** Kreuztest der BLE-Schnittstelle über eine eingefrorene Referenz-Bytefolge (`testdata/frame_v3_golden.hex`) | Firmware und App prüfen sonst nur ihre eigene Symmetrie (Encoder gegen eigenen Decoder). Ein gemeinsamer Denkfehler bliebe in beiden Testsuiten unsichtbar und fiele erst in den Felddaten auf. Der Bootstrap-Lauf schlägt bei fehlender Referenzdatei bewusst fehl | beidseitige Round-Trip-Tests als ausreichend ansehen |
| **(07.08.2026)** Zu kurzes v3-Frame wird gelesen (v2-Ebene) und in einem eigenen Zähler `truncatedV3FrameCount` geführt | Ein Gerät, das v3 meldet und zu kurz sendet, ist defekt (abgeschnittene Notification, fehlgeschlagene MTU-Aushandlung). Verwerfen vernichtete gültige v2-Nutzdaten einer Messfahrt; stillschweigendes Durchwinken verstecke den Defekt bis zur Auswertung | verwerfen; oder stillschweigend als v2 behandeln |
| **(07.08.2026)** Bei gemischten Frame-Versionen trägt die CSV-Präambel das Minimum plus `# frame_version_gemischt;ja` | Wer die Datei auswertet, muss wissen, welcher Vertrag im schwächsten Fall gilt. Das Maximum verspräche Spalten, die nicht durchgängig gefüllt sind | Maximum eintragen |
| **(07.08.2026)** `temperature_c` bleibt im Persistenzmodell der App, entfällt nur im CSV-Export | Das Feld ist weiterhin im Frame (Offset 38); es aus dem Modell zu löschen wäre eine destruktive SwiftData-Migration mit Datenverlust ohne Nutzen. E4 betraf ausdrücklich nur den Export | Feld auch aus dem Persistenzmodell entfernen |
| **(07.08.2026)** MPU6050: `DLPF_CFG` = 3 (44 Hz) und `SMPLRT_DIV` = 4 (200 Hz) fest setzen | POR-Default ist `DLPF_CFG` = 0 (260 Hz) bei `SMPLRT_DIV` = 0 (8 kHz interne Rate); `Adafruit_MPU6050::begin()` schreibt beides nie. Bei 100 Hz Auslesung ist das Unterabtastung ohne Antialiasing. 44 Hz liegt unter der halben Auslesefrequenz; 200 Hz Sensorrate vermeidet die Schwebung zwischen Sensor- und Auslesetakt, die sonst den scheinbaren Jerk verdoppelt — gerade die Größe, aus der `MOTION_NORM_JERK_DELTA` bestimmt werden soll. Messwirkung: STATIC-Anteil 68–73 % → 85,7 %. Kosten: 4,9 ms Gruppenlaufzeit gegen NFR-RT-01 ≤ 50 ms | Defaults belassen; `SMPLRT_DIV` = 9 (100 Hz, Schwebung); Data-Ready-Interrupt (als Ausblick dokumentiert) |
| **(07.08.2026)** Umfangsschnitt: die drei Normbetrags-Schwellwerte werden nicht weiter optimiert | `MOTION_NORM_STATIC_BAND`, `_JERK_DELTA` und `_SHOCK_DELTA` bleiben auf ihren physikalisch abgeleiteten Werten. Die Messfahrt dokumentiert ihr Verhalten, statt sie iterativ nachzuführen. Begründung: Für den Nachweis der Arbeit genügt der bereits erbrachte Vorher-Nachher-Beleg; eine Parametrieroptimierung wäre Produktentwicklung, nicht Thesisumfang, und bindet Zeit, die für Dokumentation und Text gebraucht wird. Eine erkannte Fehlanpassung wird als begründetes Ergebnis mit Empfehlung festgehalten | iterative Feldparametrierung der Schwellwerte |

*Nächste Entscheidungen ab hier eintragen (nach Freigabe).*
